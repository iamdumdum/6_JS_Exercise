var name = "Alice";

//only change code below this line

var firstChar = name.slice(0,1); //get the first character of the name

var middleChar = name.slice(2,3); //get the middle character of the name

var lastChar = name.slice(4,5); //get the last character of the name

//only change code above this line

module.exports = {
    firstChar, middleChar, lastChar
}